{
  "manifest_version": "0.2",
  "name": "telco-intelligence-mcp",
  "version": "2.0.0",
  "description": "Natural language interface to telco customer churn data. Query customer segments, analyse churn patterns, and predict churn risk using an ML model.",
  "author": {
    "name": "Ali"
  },
  "server": {
    "type": "python",
    "entry_point": "server.py",
    "mcp_config": {
      "command": "python",
      "args": ["${__dirname}/server.py"],
      "env": {
        "DB_URL": "${user_config.db_url}",
        "CHURN_API_URL": "http://localhost:8000"
      }
    }
  },
  "user_config": {
    "db_url": {
      "type": "string",
      "title": "Database URL",
      "description": "Leave empty to use the default telco_churn.db. Custom example: sqlite:///C:/path/to/your.db",
      "required": false,
      "sensitive": false
    }
  },
  "keywords": ["telco", "churn", "mcp", "ml", "xgboost", "sqlite", "analytics"],
  "license": "MIT"
}